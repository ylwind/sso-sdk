package site.ripic.sso.config;

/**
 * sso工厂类
 * 用于创建ssoConfig，默认读取配置文件，如果配置文件不存在，则创建默认配置
 */
public class SsoConfigFactory {

    public static SsoConfig createSsoConfig(){
        return null;
    }
}
